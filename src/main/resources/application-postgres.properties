# PostgreSQL-Konfiguration (aktiv bei Profil "postgres")
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/mealplanner
spring.datasource.username=${DB_USER:mealplanner}
spring.datasource.password=${DB_PASSWORD:mealplanner}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true